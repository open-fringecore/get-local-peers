export type TDiscoveredPeer = {
    id: string;
    name: string;
    ip: string;
    httpPort: number;
};
export type TActivePeer = {
    id: string;
    name: string;
    ip: string;
    httpPort: number;
};
